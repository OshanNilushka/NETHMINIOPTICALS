{
  "name": "nethminiopticals-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend-core && npm install && npx prisma generate",
    "start": "cd backend-core && node src/main.js"
  }
}
